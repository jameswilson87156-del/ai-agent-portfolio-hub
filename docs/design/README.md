# Design References

## Current Reference

`docs/design/references/cinematic-ai-agent-homepage-reference.png` is an AI-generated visual reference for the AI Agent Engineer Portfolio Hub homepage.

It is not a running browser screenshot, not evidence of an implemented page, and not a production UI capture.

## Screenshot Boundary

When the real webpage is implemented, verified browser screenshots must be saved under `docs/images/`.

The root `README.md` must only reference real browser screenshots generated from the app. It must not present the reference image as a finished product screenshot.

Current real browser screenshots:

- `docs/images/portfolio-home.png`
- `docs/images/large/portfolio-home.png`

## Motion References

`docs/design/motion-references/` is reserved for local motion references such as terrain videos. These files are reference material only, are ignored by Git, and must not be used as page assets.
